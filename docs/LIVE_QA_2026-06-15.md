# Live QA 2026-06-15

## Scope

- Production app: `https://beerit.vercel.app`
- Supabase project: `hbtscxousunjthuesrwb`
- Branch: `codex/live-qa-fixes`

## Findings

- Production deployment was reachable.
- Supabase project was inactive, which caused the public API hostname to fail
  DNS resolution and made data-backed routes render the app fallback.
- Restoring the Supabase project returned it to `ACTIVE_HEALTHY`.
- The applied Supabase migrations match the expected production baseline through
  `20260604220906_bomb_mode_timer_behavior`.

## Checks

- `/browse` returned HTTP 200 with full page content after Supabase restore.
- Browse content included `Browse games`, `Bomb Mode`, and `Truth or Dare`.
- Public Supabase game query returned starter games, including `Bomb Mode`.

## Follow-Up

- Keep production Supabase active before demos or testing sessions.
- If the project pauses again, restore it from the Supabase dashboard and wait
  until project status is `ACTIVE_HEALTHY`.
- The browse page now has a route-specific friendly fallback and game queries
  use timeouts, so users should not wait indefinitely on a data-backed page.
